[mysqld]
# 기본 문자셋 (이모지 포함)
character-set-server = utf8mb4
collation-server     = utf8mb4_unicode_ci

# 타임존 (한국)
default-time-zone    = Asia/Seoul

# 최대 연결 수 (개발 환경 기준 기본값보다 살짝 넉넉하게)
max_connections      = 200

# 로깅 (옵션)
slow_query_log       = 1
long_query_time      = 2
